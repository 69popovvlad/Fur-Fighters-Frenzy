<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <PropertyGroup>
    <!-- 
      Root directory to search Dependencies folder at 
    -->
    <RootDirectory>$(MSBuildProjectDirectory)\..</RootDirectory>
  </PropertyGroup>
  
</Project>